[build]
  publish = "dist/drive-flow-web"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18.19.0"

# Redirect and rewrite rules to support Angular's client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
